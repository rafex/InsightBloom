{{- if .Values.ingress.enabled -}}
{{- $webSvc := .Values.services.web -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "insightbloom.fullname" . }}
  labels:
    {{- include "insightbloom.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "insightbloom.fullname" $ }}-web
                port:
                  number: {{ $webSvc.port }}
          {{- end }}
    {{- end }}
{{- end }}
{{- if .Values.ingressChat.enabled }}
---
{{- $chatSvc := .Values.services.chat }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "insightbloom.fullname" . }}-chat
  labels:
    {{- include "insightbloom.labels" . | nindent 4 }}
    app.kubernetes.io/component: chat
  {{- with .Values.ingressChat.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingressChat.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingressChat.tls }}
  tls:
    {{- range .Values.ingressChat.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingressChat.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "insightbloom.fullname" $ }}-chat
                port:
                  number: {{ $chatSvc.port }}
          {{- end }}
    {{- end }}
{{- end }}
{{- if .Values.ingressDrawio.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "insightbloom.fullname" . }}-drawio
  labels:
    {{- include "insightbloom.labels" . | nindent 4 }}
    app.kubernetes.io/component: drawio
  {{- with .Values.ingressDrawio.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingressDrawio.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingressDrawio.tls }}
  tls:
    {{- range .Values.ingressDrawio.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingressDrawio.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              # Repuntado al gateway autenticador (no directo al pod de drawio): exige sesion
              # de InsightBloom antes de reenviar el request. Ver Values.services.toolsgateway.
              service:
                name: {{ include "insightbloom.fullname" $ }}-toolsgateway
                port:
                  number: {{ $.Values.services.toolsgateway.port }}
          {{- end }}
    {{- end }}
{{- end }}
{{- if .Values.ingressExcalidraw.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "insightbloom.fullname" . }}-excalidraw
  labels:
    {{- include "insightbloom.labels" . | nindent 4 }}
    app.kubernetes.io/component: excalidraw
  {{- with .Values.ingressExcalidraw.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingressExcalidraw.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingressExcalidraw.tls }}
  tls:
    {{- range .Values.ingressExcalidraw.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingressExcalidraw.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "insightbloom.fullname" $ }}-toolsgateway
                port:
                  number: {{ $.Values.services.toolsgateway.port }}
          {{- end }}
    {{- end }}
{{- end }}
{{- if .Values.ingressEtherpad.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "insightbloom.fullname" . }}-etherpad
  labels:
    {{- include "insightbloom.labels" . | nindent 4 }}
    app.kubernetes.io/component: etherpad
  {{- with .Values.ingressEtherpad.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingressEtherpad.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingressEtherpad.tls }}
  tls:
    {{- range .Values.ingressEtherpad.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingressEtherpad.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "insightbloom.fullname" $ }}-toolsgateway
                port:
                  number: {{ $.Values.services.toolsgateway.port }}
          {{- end }}
    {{- end }}
{{- end }}
{{- if .Values.ingressIde.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "insightbloom.fullname" . }}-ide
  labels:
    {{- include "insightbloom.labels" . | nindent 4 }}
    app.kubernetes.io/component: ide
  {{- with .Values.ingressIde.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingressIde.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingressIde.tls }}
  tls:
    {{- range .Values.ingressIde.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingressIde.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              # Mismo gateway autenticador que drawio/Etherpad, pero aca el target no es fijo:
              # AuthGateHandler lo resuelve por-sesion contra el sandbox del usuario (Fase 3b).
              service:
                name: {{ include "insightbloom.fullname" $ }}-toolsgateway
                port:
                  number: {{ $.Values.services.toolsgateway.port }}
          {{- end }}
    {{- end }}
{{- end }}
{{- if .Values.ingressTelegram.enabled }}
---
{{- $telegramSvc := .Values.services.telegram }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "insightbloom.fullname" . }}-telegram
  labels:
    {{- include "insightbloom.labels" . | nindent 4 }}
    app.kubernetes.io/component: telegram
  {{- with .Values.ingressTelegram.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingressTelegram.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingressTelegram.tls }}
  tls:
    {{- range .Values.ingressTelegram.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingressTelegram.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "insightbloom.fullname" $ }}-telegram
                port:
                  number: {{ $telegramSvc.port }}
          {{- end }}
    {{- end }}
{{- end }}
