{{- if and .Values.excalidraw.enabled .Values.autoscaling.enabled }}
{{- $minReplicas := .Values.excalidraw.autoscaling.minReplicas | default .Values.autoscaling.minReplicas }}
{{- $maxReplicas := .Values.excalidraw.autoscaling.maxReplicas | default .Values.autoscaling.maxReplicas }}
{{- $cpuTarget := .Values.excalidraw.autoscaling.targetCPUUtilizationPercentage | default .Values.autoscaling.targetCPUUtilizationPercentage }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "insightbloom.fullname" . }}-excalidraw
  labels:
    {{- include "insightbloom.labels" . | nindent 4 }}
    app.kubernetes.io/component: excalidraw
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "insightbloom.fullname" . }}-excalidraw
  minReplicas: {{ $minReplicas }}
  maxReplicas: {{ $maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ $cpuTarget }}
{{- end }}
