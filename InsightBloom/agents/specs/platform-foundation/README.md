# Platform Foundation

Spec del esqueleto operativo del repositorio InsightBloom.

## Contenido

- `SPEC.md`:
  estructura del repo, tooling operativo, CI/CD y soporte de despliegue.

## Cuando leer esta spec

- Si vas a crear el arbol base del repo.
- Si vas a definir `Justfile`, `Makefile`, `helpers-build`, `helpers-run`.
- Si vas a preparar `.github/workflows`, `infra/compose` o `infra/helm`.
