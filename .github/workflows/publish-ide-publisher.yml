name: Publish IDE publisher image

on:
  push:
    branches: [main]
    tags: ['v*.*']
    paths:
      - 'backend/services/insightbloom-ide-publisher/**'
      - '.github/workflows/publish-ide-publisher.yml'
      - '.github/workflows/_build-standalone-service.yml'
  workflow_dispatch:
    inputs:
      version_tag:
        description: 'Tag de versión'
        required: true
        type: string

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/_build-standalone-service.yml
    with:
      image_name: insightbloom-ide-publisher
      context: backend/services/insightbloom-ide-publisher
      dockerfile: backend/services/insightbloom-ide-publisher/Dockerfile
      version_tag: ${{ github.event.inputs.version_tag || '' }}
      event_name: ${{ github.event_name }}
      ref_type: ${{ github.ref_type }}
      ref_name: ${{ github.ref_name }}
    secrets: inherit
